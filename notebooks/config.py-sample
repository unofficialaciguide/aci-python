# Use this file to store your credentials for the code samples if you want.
# Replace with correct info for your environment and rename to config.py.
# We recommend testing these (or any) scripts in your lab or other non-production environment!

# All code samples assume the config.py file is located in the same directory as the code.

controller = "10.10.1.1"
username = "admin"
password = "correct horse battery staple"
